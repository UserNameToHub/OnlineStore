spring:
  application:
    name: OnlineStore

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/store
    password: postgres
    username: postgres

path:
  to:
    dir: C:/store/uploads/